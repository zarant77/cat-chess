#ifndef CAT_CHESS_UI_THEME_H
#define CAT_CHESS_UI_THEME_H

/*
 * Cat Chess UI theme tokens.
 *
 * The token values live in ui_controls.h for compatibility with the existing
 * renderer code. Include this header from new screens when you need the shared
 * color, spacing, sizing, and typography constants.
 */
#include "ui_controls.h"

#endif
